# TaxSentry npm launcher

Install with `npm install -g taxsentry`, then run `taxsentry setup`. The package installs its bundled Python core into `~/.taxsentry/runtime/venv` and forwards all commands to that isolated runtime.
